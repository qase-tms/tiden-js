{
  "name": "tiden-js",
  "private": true,
  "description": "Workspace root for the @tiden JS SDK packages (mirror of tiden-app/packages).",
  "workspaces": ["browser", "sourcemaps"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
